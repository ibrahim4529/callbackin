{
  "name": "callbackin",
  "version": "1.0.0",
  "description": "Callbackin App",
  "scripts": {
    "bootstrap": "npm run bootstrap:server",
    "server:setup": "cd server && python3 -m venv venv",
    "bootstrap:server": "npm run server:setup && cd server && source venv/bin/activate && pip install -r requirements.txt",
    "server:test": "cd server && source venv/bin/activate && python -m pytest",
    "server": "cd server && npm run"
  },
  "author": "",
  "license": "ISC"
}
